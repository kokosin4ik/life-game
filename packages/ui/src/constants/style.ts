export const FontFamily = '\'Roboto\', sans-serif';

export const ITEM_WIDTH = 328;

export enum FontSize {
    big = '20px',
    small = '14px',
}

export enum LineHeight {
    big = '28px',
    small = '20px',
}

export enum FontWeight {
    normal = 400,
    bold = 500,
}

export enum BorderRadius {
    big = '8px',
    small = '4px',
    round = '50%',
}

export enum Colors {
    white = '#FFFFFF',
    black = '#000000',
    violet = '#5A3472',
}

export enum ScrollBehavior {
    smooth = 'smooth',
}